---
name: Notion links via MCP
description: Notion pages must be read via MCP tool, not WebFetch — WebFetch cannot access authenticated Notion content
type: feedback
---

When a Jira task or any other context includes a Notion link, always read it using the Notion MCP tool instead of WebFetch. WebFetch fails on Notion because the pages require authentication.
